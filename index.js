require('dotenv').config();
const fs = require('fs');

const webserver = require('./app/webserver');
const mongo = require('./app/mongo');

//const found = findAllPathes(gates, target);
//console.log(found)

